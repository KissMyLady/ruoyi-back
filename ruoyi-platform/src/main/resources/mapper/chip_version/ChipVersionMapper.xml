<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.platform.chip.chip_version.mapper.ChipVersionMapper">

    <resultMap type="ChipVersion" id="ChipVersionResult">
            <result property="id" column="id"    />
            <result property="versionName" column="version_name"    />
            <result property="version" column="version"    />
            <result property="url" column="url"    />
            <result property="isDelete" column="is_delete"    />
            <result property="createTime" column="create_time"    />
            <result property="updateTime" column="update_time"    />
    </resultMap>

    <sql id="selectChipVersionVo">
        select id, version_name, version, url, is_delete, create_time, update_time from chip_version
    </sql>

    <select id="selectChipVersionList"
            parameterType="ChipVersion"
            resultMap="ChipVersionResult">
        <include refid="selectChipVersionVo"/>
        <where>
                        <if test="versionName != null  and versionName != ''"> and version_name like concat('%', #{versionName}, '%')</if>
                        <if test="version != null  and version != ''"> and version like '%${version}%' </if>
                        <if test="url != null  and url != ''"> and url like '%${url}%' </if>
                        <if test="isDelete != null "> and is_delete like '%${isDelete}%' </if>
        </where>
    </select>

    <select id="selectChipVersionById"
            parameterType="Long"
            resultMap="ChipVersionResult">
            <include refid="selectChipVersionVo"/>
            where id = #{id}
    </select>

    <insert id="insertChipVersion" parameterType="ChipVersion" useGeneratedKeys="true" keyProperty="id">
        insert into chip_version
        <trim prefix="(" suffix=")" suffixOverrides=",">
                    <if test="versionName != null">version_name,</if>
                    <if test="version != null">version,</if>
                    <if test="url != null">url,</if>
                    <if test="isDelete != null">is_delete,</if>
                    <if test="createTime != null">create_time,</if>
                    <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                    <if test="versionName != null">#{versionName},</if>
                    <if test="version != null">#{version},</if>
                    <if test="url != null">#{url},</if>
                    <if test="isDelete != null">#{isDelete},</if>
                    <if test="createTime != null">#{createTime},</if>
                    <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateChipVersion" parameterType="ChipVersion">
        update chip_version
        <trim prefix="SET" suffixOverrides=",">
                    <if test="versionName != null">version_name = #{versionName},</if>
                    <if test="version != null">version = #{version},</if>
                    <if test="url != null">url = #{url},</if>
                    <if test="isDelete != null">is_delete = #{isDelete},</if>
                    <if test="createTime != null">create_time = #{createTime},</if>
                    <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteChipVersionById" parameterType="Long">
        delete from chip_version where id = #{id}
    </delete>

    <delete id="deleteChipVersionByIds" parameterType="String">
        delete from chip_version where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>